<?php declare(strict_types=1);use App\Support\View;$base='/projects/'.$project->id.'/work-packages/'.$workPackage->id;$hasAllocations=$hasAllocations??false;?>
<div class="row justify-content-center"><div class="col-lg-7"><h1>Remove Work Package</h1><div class="alert alert-danger">This removes only the Work Package. It does not remove its project, responsible participant, person, or linked account.</div><?php if($hasAllocations):?><div class="alert alert-warning">This Work Package has person-hour allocations and cannot be removed. Remove or reassign them first, or deactivate the Work Package.</div><?php endif;?><div class="card card-body"><p><strong><?= View::escape($workPackage->displayTitle()) ?></strong></p><div class="d-flex gap-2"><?php if(!$hasAllocations):?><form method="post" action="<?= View::escape($urls->to($base.'/remove')) ?>"><input type="hidden" name="_csrf" value="<?= View::escape($csrfToken) ?>"><button class="btn btn-danger">Confirm removal</button></form><?php endif;?><a class="btn btn-outline-secondary" href="<?= View::escape($urls->to($base)) ?>">Cancel</a></div></div></div></div>
